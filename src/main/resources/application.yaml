spring:
  application:
    name: clientes
  datasource:
    url: jdbc:postgresql://localhost:5433/db_parkin_users
    username: parkin
    password: qwerty123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

server:
  port: 8081

logging:
  level:
    com.parkin.users: DEBUG